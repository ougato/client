// 方向类型
export enum DirectionType {
    // 水平
    HORIZONTAL = 0,
    // 垂直
    VERTICAL = 1,
}

// ListView 加载方式
export enum ListViewLoadMode {
    // 直接加载
    NONE = 0,
    // 分帧加载
    FRAME = 1,
    // 无限加载
    ENDLESS = 2,
}